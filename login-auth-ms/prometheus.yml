global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'login-auth-ms'
    static_configs:
      - targets: ['host.docker.internal:8081']

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']   # Prometheus se scrappea a sí mismo dentro del contenedor (9090)
